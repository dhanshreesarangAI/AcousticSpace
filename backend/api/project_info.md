**1. source venv/Scripts/activate**Your computer = A large apartment building 🏢Every project = One apartment 🏠Virtual environment = The apartment's private kitchen 🍳Each project has its own "ingredients" (Python packages) without affecting the others.**2.Why do you use .gitignore?**"The .gitignore file tells Git to ignore files and folders that shouldn't be tracked, such as virtual environments (venv), cache files (\_\_pycache\_\_), environment files (.env), and editor-specific settings. This keeps the repository clean and avoids committing unnecessary or sensitive files."**3.Why do you make small Git commits?**"Small commits make the project history easier to understand. If a bug appears, it's easier to identify which change introduced it and, if necessary, revert just that change."